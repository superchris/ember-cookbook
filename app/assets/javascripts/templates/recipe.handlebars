<h2>{{title}}</h2>
<p>{{description}}</p>
<h4>Ingredients</h4>
<ul class="panel">
{{#each ingredient in ingredients}}
  <li>
    {{ render "ingredient" ingredient }}
  </li>
{{/each}}
  <li><button class="button" {{action addIngredient}}>Add</button></li>
</ul>

{{#linkTo 'recipes.edit' model class="button"}}Edit{{/linkTo}}

{{outlet}}