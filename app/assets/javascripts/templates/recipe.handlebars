<h2>{{title}}</h2>
<p>{{description}}</p>
<h4>Ingredients</h4>
<ul class="panel">
{{#each ingredient in ingredients}}
  <li>
    {{ ingredient.title }}
  </li>
{{/each}}
</ul>
{{#linkTo 'recipes.edit' model class="button"}}Edit{{/linkTo}}

{{outlet}}